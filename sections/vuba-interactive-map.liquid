<section class="interactive-map bg-secondary pb-14 pt-44 md:py-14 3xl:py-24">
  <div class="installer-form padding-right-zeero pb-14 md:py-14 3xl:py-24">
    <div class="inner-container md:max-w-none flex flex-wrap">
      {% if section.settings.left_image != blank %}
        <div class="col-left rounded-3xl overflow-hidden w-full md:w-[50%] bg-white mb-12 md:mb-0">
          <img
            class="w-full"
            src="{{ section.settings.left_image | img_url: 'master' }}"
            alt="Section Image"
          >
        </div>
      {% endif %}
      <div class="col-right w-full md:w-[50%] text-center">
        {% if section.settings.heading != blank %}
          <h2 class="text-white font-bold text-2xl xl:text-3xl mb-8 2xl:mb-10 3xl:mb-20">
            {{ section.settings.heading }}
          </h2>
        {% endif %}

        <!-- Map Container -->
        <div id="map" class="w-full h-[400px] md:h-[500px] rounded-xl overflow-hidden"></div>
      </div>
    </div>
  </div>
</section>
<script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>
<script src="https://unpkg.com/leaflet.markercluster/dist/leaflet.markercluster.js"></script>
<script>
  // Initialize the map
  var map = L.map('map').setView([20, -75], 3);

  // Add a tile layer to the map
  L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'
  }).addTo(map);

  // Installer data
  var installers = [
      { name: "New York", lat: 40.7128, lng: -74.0060 },
      { name: "São Paulo", lat: -23.5505, lng: -46.6333 },
      { name: "Los Angeles", lat: 34.0522, lng: -118.2437 },
      { name: "London", lat: 51.5074, lng: -0.1278 },
      { name: "Paris", lat: 48.8566, lng: 2.3522 },
      { name: "Tokyo", lat: 35.6895, lng: 139.6917 },
      { name: "Moscow", lat: 55.7558, lng: 37.6173 },
      { name: "Sydney", lat: -33.8688, lng: 151.2093 },
      { name: "Mexico City", lat: 19.4326, lng: -99.1332 },
      { name: "Beijing", lat: 39.9042, lng: 116.4074 }
  ];

  // Create a marker cluster group
  var markers = L.markerClusterGroup();

  // Add markers to the cluster group
  installers.forEach(function(installer) {
      var marker = L.marker([installer.lat, installer.lng]);
      marker.bindPopup("<b>" + installer.name + "</b>");
      markers.addLayer(marker);
  });

  // Add the marker cluster group to the map
  map.addLayer(markers);

  // Handle dropdown change
  document.getElementById('locationDropdown').addEventListener('change', function(e) {
      var selected = e.target.value;
      if (selected) {
          var location = installers.find(i => i.name === selected);
          if (location) {
              map.setView([location.lat, location.lng], 10);
          }
      }
  });
</script>

{% schema %}
{

  "name": "Vuba Map",

  "settings": [

    {

      "type": "text",

      "id": "heading",

      "label": "Heading",

      "default": "Explore our interactive map"

    },

    {

      "type": "image_picker",

      "id": "left_image",

      "label": "Left Side Image"

    },

    {

      "type": "image_picker",

      "id": "right_image",

      "label": "Right Side Image"

    },

    {

      "type": "text",

      "id": "button_text",

      "label": "Button Text",

      "default": "VIEW DEALER LIST"

    }, {

      "type": "image_picker",

      "id": "button_icon",

      "label": "Button Icon"

    }

  ],

  "blocks": [

    {

      "type": "dealer",

      "name": "Dealer",

      "settings": [

        {

          "type": "text",

          "id": "country",

          "label": "Country"

        },

        {

          "type": "text",

          "id": "role",

          "label": "Role",

          "default": "Exclusive Distributor / Contractor"

        },

        {

          "type": "text",

          "id": "company_name",

          "label": "Company Name"

        },

        {

          "type": "text",

          "id": "contact_name",

          "label": "Contact Name"

        }, {

          "type": "text",

          "id": "phone",

          "label": "Phone Number"

        }, {

          "type": "url",

          "id": "website",

          "label": "Website URL"

        }, {

          "type": "text",

          "id": "email",

          "label": "Email"

        }, {

          "type": "url",

          "id": "instagram",

          "label": "Instagram URL"

        }

      ]

    }

  ],

  "presets": [

    {

      "name": "Vuba Map",

      "category": "Text",

      "blocks": [

        {

          "type": "dealer",

          "settings": {

            "country": "Bulgaria",

            "company_name": "M Diva Green Ltd",

            "contact_name": "Elina Hadzhieva",

            "phone": "+359878709988",

            "website": "https://mdivagreen.com",

            "email": "m.diva.green@gmail.com",

            "instagram": "https://instagram.com/m.diva.green"

          }

        }

      ]

    }

  ]

}
{% endschema %}
